"use client";
import { useState } from "react";
import { createClient } from "@/lib/supabase/client";
import { Activity, Chrome, Leaf } from "lucide-react";
export default function LoginPage(){const supabase=createClient();const[register,setRegister]=useState(false);const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const[loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError("");const result=register?await supabase.auth.signUp({email,password}):await supabase.auth.signInWithPassword({email,password});setLoading(false);if(result.error)setError(result.error.message);else if(register) setError("Cek email Anda untuk konfirmasi akun.");else location.href="/dashboard"}async function google(){await supabase.auth.signInWithOAuth({provider:"google",options:{redirectTo:`${location.origin}/auth/callback`}})}return <main className="min-h-screen grid lg:grid-cols-2"><section className="hidden lg:flex p-16 bg-[#123b31] text-white flex-col justify-between"><div className="flex items-center gap-3 font-black text-xl"><span className="grid place-items-center size-10 rounded-xl bg-[#25c58f]"><Leaf size={22}/></span>CalorieTrack.ai</div><div><p className="text-[#54d8aa] font-bold tracking-widest text-sm">NUTRISI LEBIH MUDAH</p><h1 className="text-5xl font-black leading-tight mt-4">Kenali makananmu.<br/>Capai targetmu.</h1><p className="text-white/65 text-lg max-w-lg mt-5">Foto makanan atau ceritakan porsinya. AI membantu memperkirakan kalori dan makro dalam hitungan detik.</p></div><div className="flex gap-3 text-sm text-white/60"><Activity size={18}/> Analisis cerdas, progres yang terasa.</div></section><section className="grid place-items-center p-6"><div className="card w-full max-w-md p-8"><div className="lg:hidden flex items-center gap-2 font-black text-xl mb-8"><Leaf className="text-[#12a775]"/>CalorieTrack.ai</div><p className="text-sm font-bold text-[#0b966a]">SELAMAT DATANG</p><h2 className="text-3xl font-black mt-2">{register?"Buat akun baru":"Masuk ke akun Anda"}</h2><p className="text-[#6b7c76] mt-2 mb-7">{register?"Mulai perjalanan sehat Anda hari ini.":"Lanjutkan pencatatan nutrisi harian Anda."}</p><form onSubmit={submit} className="space-y-4"><label className="block"><span className="label">EMAIL</span><input className="field mt-2" type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="nama@email.com"/></label><label className="block"><span className="label">PASSWORD</span><input className="field mt-2" type="password" minLength={6} required value={password} onChange={e=>setPassword(e.target.value)} placeholder="Minimal 6 karakter"/></label>{error&&<p className="text-sm text-[#b85640] bg-[#fff1ed] p-3 rounded-xl">{error}</p>}<button disabled={loading} className="primary w-full">{loading?"Memproses...":register?"Daftar":"Masuk"}</button></form><div className="flex items-center gap-3 my-5 text-xs text-[#82918c]"><span className="h-px bg-[#dce7e2] flex-1"/>ATAU<span className="h-px bg-[#dce7e2] flex-1"/></div><button onClick={google} className="secondary w-full flex justify-center items-center gap-2"><Chrome size={18}/> Lanjutkan dengan Google</button><p className="text-sm text-center mt-7 text-[#6b7c76]">{register?"Sudah punya akun?":"Belum punya akun?"} <button onClick={()=>{setRegister(!register);setError("")}} className="font-bold text-[#0b966a]">{register?"Masuk":"Daftar sekarang"}</button></p></div></section></main>}
